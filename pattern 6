#include <stdio.h>

int main() {
    int a, b, c;

    scanf("%d", &b);

    for (a = 1; a <= b; a++) {
        for (c = 1; c <= a; c++) {
            printf("%d",a);
        }
        printf("\n");
    }

    return 0;
}
